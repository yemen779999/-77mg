# دليل تشغيل لوحة تحكم الويندوز (Admin Dashboard) والاتصال السحابي 💻☁️

هناك طريقتان لتشغيل لوحة تحكّم النظام ومراقبة العملاء والأجهزة من جهاز كمبيوتر يعمل بالويندوز:

---

## الطريقة الأولى: الموصى بها لسهولة التثبيت الفورية (بدون متطلبات بايثون) 🆕🟢

لتسهيل تجربة لوحة التحكم عليك وحمايتك من مشاكل منافذ الاستقبال أو تداخل أكواد بايثون على جهازك، قمنا بابتكار **لوحة تحكّم تفاعلية ممتازة بصيغة HTML (اسم الملف: `AnasPro_Windows_Admin.html`)** تعمل على أي جهاز ويندوز بضغطة زر وبدون تثبيت أي برامج مسبقة!

### مميزات هذه الطريقة:
1. **تشغيل فوري:** فقط قم بنقل ملف `AnasPro_Windows_Admin.html` إلى جهاز الكمبيوتر الخاص بك.
2. **شغل مباشرة:** انقر فوق الملف نقرتين متتاليتين (Double-Click) وسياخذك المتصفح (Edge أو Chrome أو Firefox) فوراً لأجمل لوحة تحكّم Material 3 باللغة العربية.
3. **دعم كامل لحفظ وتعديل البيانات:** تدعم اللوحة التعديلات والتحكم بالأجهزة وتفعيل/تجميد الحسابات السحابية كلياً وتحتفظ بها داخل متصفحك بشكل تفاعلي وآمن.

---

## الطريقة الثانية: تشغيل لوحة التحكم المتقدمة بلغة Python (بمكتبة Flet)

إذا كنت ترغب بتشغيل ملف البايثون المتقدم `AnasPro_Windows_Admin.py` والاستمتاع بواجهاته الرائعة، اتبع الآتي:

### متطلبات نظام التشغيل لسطح المكتب:
1. **تثبيت لغة بايثون (Python):**
   - قم بتحميل وتثبيت بايثون من الموقع الرسمي: [python.org](https://www.python.org/downloads/)
   - **هام جداً:** أثناء التثبيت، احرص على تفعيل ميزة **"Add Python to PATH"** لكي تتمكن من كتابة الأمر `python` في منفذ الأوامر مباشرة.

2. **تثبيت المكتبات البرمجية اللازمة:**
   افتح موجه الأوامر برتبة مسؤول (CMD أو PowerShell) على الويندوز واكتب الأمر التالي:
   ```bash
   pip install flet requests
   ```

3. **تشغيل البرنامج:**
   انتقل إلى المجلد الذي يحتوي على كود بايثون واكتب الأمر التالي:
   ```bash
   flet run AnasPro_Windows_Admin.py
   ```
   *سيفتح لك تطبيق سطح مكتب مستقل واحترافي ورائع جداً للتحكم بالمستخدمين والعملاء.*

---

## ثانياً: كيفية عمل ميزة التجميد (Account Freezing) ومحاكاتها بالكامل 🛑

تطبيق الأندرويد يدعم التحكم بالسحاب كاملاً:
- عندما يقوم العميل بالضغط على **"مزامنة ورفع البيانات سحابياً"** يرسل التطبيق كافة الحسابات والعمليات مشفرةً ومعبأةً في صيغ JSON مضغوطة تحت **المعرّف الخاص به (Client ID)**.
- **تأثير التجميد على تطبيق الهاتف:**
  إذا قمت بتجميد حساب معين من لوحة تحكم بايثون (أو للتجربة قمت بتغيير المعرّف في الهاتف إلى صيغة تحتوي على الكلمة `"freeze"` أو `"تجميد"`)، بمجرد الضغط على مزامنة يتواصل الجهاز مع السيرفر ويرى أن حالته **"مجمد"**!
  مباشرةً، يقوم تطبيق الأندرويد بقفل ومنع الوصول إلى الواجهات الحساسة (الرئيسية، الحسابات، العمليات، المستشار الذكي)، ويعرض شاشة حمراء واضحة للزبون: **"عذراً، هذا الحساب مجمد! 🛑 الرجاء التواصل مع المطور/المشرف لإعادة تفعيل حسابك"**.
  *ويظل الهاتف مغلقاً حتى يعاد تفعيله مجدداً من قبلك في السحاب ثم المزامنة، أو حين يضيف العميل معرفاً مرخصاً وصحيحاً.*

---

## ثالثاً: خيارات الاتصال الفعلي وإعداد قاعدة البيانات المشتركة (Firebase / Supabase)

الكود البرمجي في الأندرويد مهيأ بالكامل ليدعم عمليتي المحاكاة الافتراضية الذكية والاتصال الحقيقي:

### 1. الاستخدام الافتراضي المدمج (بدون تعقيد)
يعمل المخدم الافتراضي على المحاكاة السحابية لكي تضمن عمل ومظهر التطبيق بنسبة 100% في المحاكاة التجريبية وحفظ السجلات في الذاكرة المشتركة.

### 2. التفعيل الحقيقي لقواعد قواعد البيانات (Supabase / Firebase REST Real Sync)
إذا أردت ربطه بقاعدة بيانات سحابية فعلية مجانية مثل Supabase:
- أنشئ مشروعاً جديداً في [Supabase](https://supabase.com) أو Firebase.
- احصل على رابط الـ API Endpoint الخاص بك.
- في شاشة إعدادات الأندرويد، اضغط على **"تعديل المعرّف"** وضع عنوان الرابط السحابي المناسب الخاص بجهات السيرفر الخاصة بك في حقل **"رابط السحاب"**.
- ستقوم دوال `syncWithCloud` بإرسال البيانات الفروع والكبائن عبر standard REST API بطلب POST مباشر يسهل على أي خادم بايثون SQL أو Firebase استقباله وحفظه وقراءته!
